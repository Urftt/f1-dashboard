---
phase: 01-backend-foundation
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-03"]
files_modified: []
autonomous: false
requirements:
  - SESS-01
  - SESS-02
  - SESS-03
  - SESS-04
  - SESS-05

must_haves:
  truths:
    - "User can pick a season, browse events, select session type from working UI"
    - "User sees real-time progress during a real FastF1 session load"
    - "Cached session reloads instantly (under 1 second)"
    - "Lap data is confirmed present in Zustand store via browser dev console"
  artifacts: []
  key_links: []
---

<objective>
End-to-end integration verification of the full session loading pipeline.

Purpose: Confirm that the entire Phase 1 deliverable works as an integrated system — frontend talks to backend, SSE streams correctly, caching works, and lap data lands in the Zustand store. This is the phase gate checkpoint.

Output: Verified working system ready for Phase 2.
</objective>

<execution_context>
@/Users/luckleineschaars/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luckleineschaars/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-backend-foundation/01-CONTEXT.md
@.planning/phases/01-backend-foundation/01-01-SUMMARY.md
@.planning/phases/01-backend-foundation/01-02-SUMMARY.md
@.planning/phases/01-backend-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: End-to-end integration verification</name>
  <action>
    Human verifies the complete session loading pipeline works end-to-end.

    What was built: Complete session loading pipeline — cascading session selector, SSE progress streaming, FastF1 data loading with caching, Zustand state management.

    Steps to verify:

    1. Start backend: `cd backend && uv run uvicorn main:app --port 8000 --reload`
    2. Start frontend: `cd frontend && npm run dev`
    3. Open http://localhost:5173 in browser

    **Test session selection (SESS-01, SESS-02, SESS-03):**
    4. Verify Year dropdown defaults to current season (2026) and contains years 2018-2026
    5. Verify Event dropdown populates with completed Grand Prix events (no future races)
    6. Select an event — verify Session Type dropdown shows available types and defaults to "Race"

    **Test loading progress (SESS-04):**
    7. Click "Load Session" — verify inline progress bar appears below selector
    8. Verify progress shows percentage AND stage name (e.g., "20% — Fetching session info...")
    9. Wait for load to complete (30-120 seconds on first load)

    **Test cache (SESS-05):**
    10. After first load completes, click "Change" to expand selector
    11. Select the same session again and click "Load Session"
    12. Verify it completes in under 1 second (cached)
    13. Check that the event shows a cached indicator in the dropdown

    **Test state and UI:**
    14. After load: verify selector compacts to summary line (e.g., "Monaco GP 2024 — Race")
    15. Open browser dev console or React DevTools — verify Zustand store has populated laps array
    16. Verify empty state message shows on initial page load before any session is selected

    **Test error handling:**
    17. Try loading a session that might fail (e.g., a very recent event with no data yet) — verify error message appears inline with retry button

    Expected: All steps pass. The session loading pipeline is fully functional.
  </action>
  <verify>Human confirms all verification steps pass</verify>
  <done>All four ROADMAP Phase 1 success criteria confirmed: session selection works, progress bar streams, cache is instant, laps in store</done>
</task>

</tasks>

<verification>
Human verification of end-to-end integration. All Phase 1 success criteria from ROADMAP.md checked.
</verification>

<success_criteria>
- All four ROADMAP success criteria confirmed by human testing:
  1. User can pick season, browse events, select session type
  2. Loading progress bar updates in real time
  3. Cached sessions load instantly
  4. Lap data available in frontend store
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-foundation/01-04-SUMMARY.md`
</output>
