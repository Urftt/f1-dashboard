---
phase: 02-gap-chart-replay-engine
plan: 04
type: execute
wave: 3
depends_on: ["02-02", "02-03"]
files_modified:
  - frontend/src/components/Dashboard/Dashboard.tsx
  - frontend/src/App.tsx
  - frontend/src/index.css
autonomous: false
requirements:
  - GAP-01
  - GAP-02
  - GAP-03
  - REPL-01
  - REPL-02
  - REPL-03
  - REPL-04

must_haves:
  truths:
    - "User sees two-column layout: gap chart on left (~60%), standings placeholder on right (~40%)"
    - "Session selector and replay controls are sticky at the top"
    - "Driver dropdowns sit above the gap chart in the left column"
    - "Entire dashboard uses dark theme (not just the chart)"
    - "After session load, chart renders immediately with auto-selected P1/P2 drivers"
    - "Play/pause advances the chart cursor lap by lap"
    - "Scrubber drag jumps cursor to target lap"
  artifacts:
    - path: "frontend/src/components/Dashboard/Dashboard.tsx"
      provides: "Two-column layout with gap chart left and placeholder right"
      min_lines: 20
    - path: "frontend/src/App.tsx"
      provides: "Refactored layout with sticky header and dashboard body"
      min_lines: 20
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/components/ReplayControls/ReplayControls.tsx"
      via: "Rendered in sticky header below SessionSelector"
      pattern: "ReplayControls"
    - from: "frontend/src/components/Dashboard/Dashboard.tsx"
      to: "frontend/src/components/GapChart/GapChart.tsx"
      via: "Rendered in left column"
      pattern: "GapChart"
    - from: "frontend/src/components/Dashboard/Dashboard.tsx"
      to: "frontend/src/components/GapChart/DriverSelector.tsx"
      via: "Rendered above GapChart in left column"
      pattern: "DriverSelector"
---

<objective>
Wire all Phase 2 components into the dashboard layout. Refactor App.tsx to use a sticky header (compact session selector + replay controls) with a two-column body below. Apply dark theme globally. Verify the complete feature end-to-end with a human checkpoint.

Purpose: This is the integration plan — all individual components exist, now they must be assembled into the user-facing product. The human checkpoint confirms the core product loop works: select drivers, see gap chart, replay with cursor tracking.
Output: Working dashboard with gap chart + replay engine; human-verified.
</objective>

<execution_context>
@/Users/luckleineschaars/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luckleineschaars/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-gap-chart-replay-engine/02-CONTEXT.md
@.planning/phases/02-gap-chart-replay-engine/02-01-SUMMARY.md
@.planning/phases/02-gap-chart-replay-engine/02-02-SUMMARY.md
@.planning/phases/02-gap-chart-replay-engine/02-03-SUMMARY.md

<interfaces>
<!-- Components from Plans 02 and 03 that this plan wires together -->

From frontend/src/components/GapChart/GapChart.tsx (Plan 02):
```typescript
// Self-contained chart component — reads from store, no props needed
export function GapChart(): JSX.Element
```

From frontend/src/components/GapChart/DriverSelector.tsx (Plan 02):
```typescript
// Self-contained driver selection — reads/writes store, no props needed
export function DriverSelector(): JSX.Element
```

From frontend/src/components/ReplayControls/ReplayControls.tsx (Plan 03):
```typescript
// Self-contained replay bar — reads/writes store, no props needed
// Returns null when stage !== 'complete'
export function ReplayControls(): JSX.Element | null
```

From frontend/src/components/SessionSelector/SessionSelector.tsx (existing Phase 1):
```typescript
export function SessionSelector(): JSX.Element
```

From frontend/src/components/Dashboard/EmptyState.tsx (existing Phase 1):
```typescript
export function EmptyState(): JSX.Element
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Dashboard layout and refactor App.tsx with dark theme</name>
  <files>frontend/src/components/Dashboard/Dashboard.tsx, frontend/src/App.tsx, frontend/src/index.css</files>
  <action>
1. **Apply dark theme globally** (per user decision: "entire dashboard uses dark theme, not just the chart"):
   - In `frontend/src/index.css`, add `html { color-scheme: dark; }` or add the `dark` class to `<html>` in `index.html`
   - Ensure shadcn/ui dark theme variables are active. The shadcn/ui setup likely uses CSS variables — check if a `.dark` class is needed on `<html>` or `<body>`, or if `prefers-color-scheme` handles it. Force dark mode regardless of system preference.
   - Background should be a dark color (the existing `bg-background` class should resolve to dark with the dark theme active)

2. **Create `frontend/src/components/Dashboard/Dashboard.tsx`**:
   - Two-column layout per user decision: gap chart on left (~60%), standings placeholder on right (~40%)
   - Left column contains:
     - `<DriverSelector />` at the top
     - `<GapChart />` below filling remaining height
   - Right column contains:
     - A placeholder card: "Standings coming in Phase 3" (or similar — subtle, not distracting)
     - Style: `bg-card border rounded-lg` with muted text
   - Use CSS grid or flexbox: `grid grid-cols-5 gap-4` where left gets `col-span-3` and right gets `col-span-2` (achieves ~60/40 split)
   - On smaller screens, stack columns vertically (responsive: `grid-cols-1 lg:grid-cols-5`)
   - Only render when `stage === 'complete'`

3. **Refactor `frontend/src/App.tsx`**:
   - Sticky header area containing:
     - `<SessionSelector />` (compact mode — already auto-compacts when session loads)
     - `<ReplayControls />` below the selector (only shows when session loaded)
     - Sticky classes: `sticky top-0 z-10 bg-background/95 backdrop-blur border-b`
   - Body area below the sticky header:
     - When `stage === 'idle'`: show `<EmptyState />`
     - When `stage === 'loading'`: show `<LoadingProgress />`
     - When `stage === 'complete'`: show `<Dashboard />`
   - Remove the old placeholder card that showed "Session loaded: X laps from Y drivers"
   - Remove the `max-w-4xl` constraint — the dashboard needs full width (use `max-w-7xl` or similar for wider layout)
   - Keep the app title "F1 Dashboard" in the sticky header area

4. **Ensure all imports are correct** — GapChart, DriverSelector, ReplayControls, Dashboard are all imported from their respective paths.
  </action>
  <verify>
    <automated>cd /Users/luckleineschaars/repos/f1-dashboard/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Dashboard renders two-column layout with GapChart left and placeholder right; App.tsx has sticky header with SessionSelector and ReplayControls; dark theme applied globally; old placeholder removed; full-width layout</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete Phase 2 gap chart and replay engine</name>
  <action>
Human verifies the complete Phase 2 integration: driver selection, gap chart, replay controls, and dashboard layout. All automated work is done in Task 1 — this checkpoint confirms everything works end-to-end.

What was built: Driver selection dropdowns, interactive gap-over-time Plotly chart with dark theme and team-color lines, play/pause replay with speed control, lap scrubber, and vertical cursor tracking the current lap.

How to verify:
1. Start both servers:
   ```bash
   cd backend && uvicorn main:app --reload &
   cd frontend && npm run dev
   ```
   Open http://localhost:5173

2. **Session loading**: Select a season (e.g. 2024), a Grand Prix, and "Race". Wait for the session to load. Verify loading progress still works.

3. **Auto-selection (GAP-01)**: After load, verify two drivers are auto-selected (grid P1 and P2). The gap chart should render immediately — no empty state.

4. **Gap chart (GAP-02)**: Verify the chart shows gap-over-time with:
   - Dark background (Plotly dark theme)
   - Zero-line reference
   - Line color changes based on who is leading (team colors)
   - Chart fills the left ~60% of the layout

5. **Tooltip (GAP-03)**: Hover over the chart line. Verify tooltip shows "Lap N: +X.XXXs" format.

6. **Driver selection (GAP-01)**: Change Driver A and Driver B in the dropdowns. Verify:
   - Dropdowns show "ABB — Full Name" format
   - Items are grouped by team
   - Chart updates immediately on selection change

7. **Play/pause (REPL-01)**: Press play. Verify the vertical dashed cursor advances lap by lap. Press pause — cursor freezes.

8. **Speed (REPL-02)**: Try different speeds (0.5x, 1x, 2x, 4x). Verify 4x is noticeably faster than 1x.

9. **Scrubber (REPL-03)**: Drag the scrubber to a random lap. Verify the cursor jumps to that lap instantly.

10. **Cursor (REPL-04)**: Verify the dashed vertical line on the chart matches the current lap number shown in the "Lap X/Y" counter.

11. **Layout**: Verify:
    - Session selector and replay controls are sticky at the top
    - Two-column layout: chart left, "standings coming soon" placeholder right
    - Entire dashboard is dark themed

12. **No spoilers**: Verify the driver dropdowns do NOT reveal finishing order.
  </action>
  <verify>
    <automated>cd /Users/luckleineschaars/repos/f1-dashboard/frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Human confirmed all 7 requirements work: GAP-01 (driver selection), GAP-02 (gap chart), GAP-03 (tooltips), REPL-01 (play/pause), REPL-02 (speed), REPL-03 (scrubber), REPL-04 (cursor). Resume signal: "approved" or describe issues.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` passes
- `cd frontend && npm run build` succeeds (production build test)
- `cd backend && python -m pytest tests/ -x -q` passes (no backend regressions)
- Human verifies all 7 requirements (GAP-01 through GAP-03, REPL-01 through REPL-04)
</verification>

<success_criteria>
- Two-column dashboard layout with sticky header
- Gap chart renders with dark theme, team colors, zero-line, and tooltip
- Replay controls with play/pause, speed selection, scrubber, lap counter
- Chart cursor tracks current replay lap
- Auto-selected P1/P2 drivers on session load
- No spoilers in driver ordering
- Human approved after smoke test
</success_criteria>

<output>
After completion, create `.planning/phases/02-gap-chart-replay-engine/02-04-SUMMARY.md`
</output>
