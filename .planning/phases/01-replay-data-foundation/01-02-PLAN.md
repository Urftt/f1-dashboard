---
phase: 01-replay-data-foundation
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - replay_data.py
  - data_processor.py
  - app.py
autonomous: true
requirements:
  - REPL-01
  - STAT-02
  - STAT-03
user_setup: []
must_haves:
  truths:
    - "At a chosen replay lap, the app can derive the current tyre compound for a selected driver."
    - "At a chosen replay lap, the app can derive the current tyre age for a selected driver."
    - "The Streamlit flow uses replay helpers instead of embedding tyre snapshot logic directly inside the blocking tracking loop."
  artifacts:
    - path: "data_processor.py"
      provides: "Pure replay snapshot helpers for tyre-aware KPI derivation."
      min_lines: 120
      contains: "get_driver_snapshot"
    - path: "app.py"
      provides: "Streamlit wiring that reads replay preload data and renders tyre-aware stats."
      contains: "current_compound"
    - path: "replay_data.py"
      provides: "Replay model helpers consumed by snapshot calculations."
      contains: "lap_number"
  key_links:
    - from: "app.py"
      to: "data_processor.py"
      via: "Streamlit metric flow calls pure replay snapshot helpers"
      pattern: "get_driver_snapshot|get_replay_snapshot"
    - from: "data_processor.py"
      to: "replay_data.py"
      via: "snapshot logic reads normalized replay session rows"
      pattern: "ReplaySession|ReplayLap"
---

<objective>
Implement tyre-aware replay snapshot helpers and integrate them into the Streamlit historical-session flow.

Purpose: The dashboard’s replay value depends on being able to derive compound and tyre age from normalized historical data, outside the UI loop.
Output: Pure snapshot helpers plus Streamlit integration that surfaces replay-derived tyre stats from the canonical preload path.
</objective>

<execution_context>
@/home/james-turing/.codex/get-shit-done/workflows/execute-plan.md
@/home/james-turing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-replay-data-foundation/01-RESEARCH.md
@.planning/phases/01-replay-data-foundation/01-VALIDATION.md
@.planning/phases/01-replay-data-foundation/01-01-SUMMARY.md
@app.py
@data_processor.py
@replay_data.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pure replay snapshot helpers for driver KPIs</name>
  <files>data_processor.py, replay_data.py</files>
  <action>Extend the processing layer with pure helpers that accept normalized replay session data plus a replay position and return per-driver snapshot values. Implement helpers for latest known lap row lookup, current tyre compound, current tyre age, and any needed fallback semantics when source fields are missing. Keep the helpers independent from Streamlit state and preserve the existing interval calculator behavior for later phases.</action>
  <verify>python -m py_compile data_processor.py replay_data.py</verify>
  <done>The processing layer exposes replay snapshot functions that can be called directly in tests or the UI without entering a tracking loop.</done>
</task>

<task type="auto">
  <name>Task 2: Route historical-session UI state through the replay foundation</name>
  <files>app.py, data_processor.py</files>
  <action>Refactor the historical session load branch in `app.py` to store the canonical replay session object from Plan 01, derive available drivers from that contract, and call the new snapshot helpers for current tyre-aware KPIs. Keep the current Streamlit layout intact, but reduce direct dependence on raw `st.session_state` DataFrames so later replay control work can advance from a stable foundation.</action>
  <verify>python -m py_compile app.py data_processor.py</verify>
  <done>The app’s historical-session flow reads from the replay preload contract and can surface compound and tyre-age values for the selected drivers.</done>
</task>

<task type="auto">
  <name>Task 3: Define replay-position semantics in the UI seam</name>
  <files>app.py, data_processor.py</files>
  <action>Make the phase decision explicit that replay position is lap-granular for now. Use whichever current lap source is most reliable in the existing app to feed the snapshot helpers, and add narrow fallbacks so missing tyre data produces predictable placeholders rather than crashes or misleading values.</action>
  <verify>rg -n "tyre|compound|age|replay" app.py data_processor.py</verify>
  <done>The Streamlit layer has an explicit, non-ambiguous way to ask for tyre-aware replay stats at the current replay position.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m py_compile app.py data_processor.py replay_data.py` passes.
- [ ] Manual Streamlit smoke check shows a historical session can still load without errors.
- [ ] Selected-driver metrics use replay snapshot helpers for tyre compound and tyre age instead of ad hoc widget-level calculations.
</verification>

<success_criteria>
- All tasks completed
- `STAT-02` and `STAT-03` have a real implementation seam in pure Python helpers
- Historical session loading remains usable after the refactor
- Phase 2 can build replay controls on top of lap-granular snapshot APIs rather than direct widget state
</success_criteria>

<output>
After completion, create `.planning/phases/01-replay-data-foundation/01-02-SUMMARY.md`
</output>
