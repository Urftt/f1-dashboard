---
phase: 01-replay-data-foundation
plan: "03"
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - pyproject.toml
  - tests/conftest.py
  - tests/fixtures/replay_session.json
  - tests/test_replay_foundation.py
autonomous: true
requirements:
  - RELY-03
  - STAT-02
  - STAT-03
user_setup: []
must_haves:
  truths:
    - "Core replay preload and snapshot calculations are covered by automated tests that run without hitting OpenF1."
    - "Fixture-backed tests prove tyre compound and tyre age semantics at replay positions, including missing-data fallbacks."
    - "The repo has a documented pytest entrypoint for Phase 1 replay foundation work."
  artifacts:
    - path: "tests/test_replay_foundation.py"
      provides: "Automated replay foundation coverage for preload and tyre-aware calculations."
      min_lines: 120
      contains: "test_"
    - path: "tests/conftest.py"
      provides: "Shared replay fixture builders and DataFrame helpers."
      contains: "pytest.fixture"
    - path: "tests/fixtures/replay_session.json"
      provides: "Deterministic recorded lap fixture with compound changes and fallback cases."
      contains: "lap_number"
    - path: "pyproject.toml"
      provides: "Pytest development dependency and runnable test configuration."
      contains: "pytest"
  key_links:
    - from: "tests/test_replay_foundation.py"
      to: "replay_data.py"
      via: "fixture-backed preload normalization tests"
      pattern: "ReplaySession|normalize|preload"
    - from: "tests/test_replay_foundation.py"
      to: "data_processor.py"
      via: "snapshot helper assertions for compound and tyre age"
      pattern: "compound|age|snapshot"
---

<objective>
Add the automated test infrastructure and deterministic fixtures that validate replay preload and tyre-aware snapshot semantics.

Purpose: Phase 1 is not complete without repeatable coverage for the replay calculations that later phases will depend on.
Output: A pytest-backed replay test suite, reusable fixtures, and a repo-level test entrypoint that runs without live API calls.
</objective>

<execution_context>
@/home/james-turing/.codex/get-shit-done/workflows/execute-plan.md
@/home/james-turing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-replay-data-foundation/01-RESEARCH.md
@.planning/phases/01-replay-data-foundation/01-VALIDATION.md
@.planning/phases/01-replay-data-foundation/01-01-SUMMARY.md
@.planning/phases/01-replay-data-foundation/01-02-SUMMARY.md
@pyproject.toml
@data_fetcher.py
@data_processor.py
@replay_data.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install pytest support for replay-foundation work</name>
  <files>pyproject.toml</files>
  <action>Add `pytest` to the development dependency group and, if useful, minimal pytest configuration that keeps discovery simple for this small repo. Do not introduce broader tooling churn; keep the change focused on making the replay foundation suite runnable from `python -m pytest` as described in the validation contract.</action>
  <verify>rg -n "pytest" pyproject.toml</verify>
  <done>The repo advertises a concrete pytest entrypoint for replay foundation tests.</done>
</task>

<task type="auto">
  <name>Task 2: Create deterministic replay fixtures with tyre-change coverage</name>
  <files>tests/conftest.py, tests/fixtures/replay_session.json</files>
  <action>Create a compact fixture set that models two-driver lap progression, a compound change after a pit stop, and missing tyre fields. Provide helpers that load the fixture into the same replay-normalization and snapshot paths the app uses, so tests exercise the real contracts instead of bespoke mocks.</action>
  <verify>python -m py_compile tests/conftest.py</verify>
  <done>Tests can construct replay-ready session data from deterministic local fixtures with no network access.</done>
</task>

<task type="auto">
  <name>Task 3: Add fixture-backed tests for preload and tyre-aware semantics</name>
  <files>tests/test_replay_foundation.py</files>
  <action>Write automated tests for replay preload normalization, latest-lap snapshot lookup, tyre compound lookup, tyre age lookup, and missing-field fallback behavior. Include at least one regression-style test that proves the replay helpers are callable independently of the Streamlit loop and one test that confirms duplicate lap rows are normalized deterministically.</action>
  <verify>python -m pytest tests/test_replay_foundation.py</verify>
  <done>The core replay semantics required by Phase 1 are protected by fixture-backed automated tests and match the validation strategy.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest tests/test_replay_foundation.py` passes.
- [ ] `python -m pytest` passes or only fails on unrelated pre-existing issues documented in the summary.
- [ ] Tests exercise replay preload and snapshot helpers without calling the live OpenF1 API.
</verification>

<success_criteria>
- All tasks completed
- `RELY-03` is satisfied with deterministic local coverage
- Fixture data covers compound changes and incomplete tyre fields
- The replay foundation has a fast regression suite for subsequent phases
</success_criteria>

<output>
After completion, create `.planning/phases/01-replay-data-foundation/01-03-SUMMARY.md`
</output>
