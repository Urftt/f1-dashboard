---
phase: 01-replay-data-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - replay_data.py
  - data_fetcher.py
autonomous: true
requirements:
  - REPL-01
user_setup: []
must_haves:
  truths:
    - "A historical session can be loaded into a reusable replay session object without depending on Streamlit widget state."
    - "Replay preload returns stable, driver-scoped lap data ordered by lap number and timestamp."
    - "The load path exposes enough normalized metadata for later tyre-aware snapshot calculations."
  artifacts:
    - path: "replay_data.py"
      provides: "Replay session dataclasses and normalization helpers for historical session preload."
      min_lines: 80
      contains: "class ReplaySession"
    - path: "data_fetcher.py"
      provides: "Fetcher entrypoints that build replay-ready session payloads from OpenF1 data."
      contains: "load_replay_session"
  key_links:
    - from: "data_fetcher.py"
      to: "replay_data.py"
      via: "historical session load path calls normalization helpers"
      pattern: "load_replay_session|normalize"
---

<objective>
Extract the historical session load path from the Streamlit event flow and normalize it into a replay-ready session model.

Purpose: Phase 1 needs a reusable replay data foundation before tyre-aware KPIs and tests can be added safely.
Output: A replay-session module plus fetcher entrypoints that preload session, driver, and lap data in a deterministic shape.
</objective>

<execution_context>
@/home/james-turing/.codex/get-shit-done/workflows/execute-plan.md
@/home/james-turing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-replay-data-foundation/01-RESEARCH.md
@.planning/phases/01-replay-data-foundation/01-VALIDATION.md
@data_fetcher.py
@app.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define a canonical replay session model</name>
  <files>replay_data.py</files>
  <action>Create a dedicated replay data module with lightweight dataclasses or typed containers for session metadata, drivers, and normalized lap rows. Add normalization helpers that coerce datetime fields, sort laps deterministically, collapse duplicate driver/lap rows, and retain tyre-related source fields without tying the contract to Streamlit session state.</action>
  <verify>python -m py_compile replay_data.py</verify>
  <done>The repo has a replay session contract that can represent one loaded historical session with normalized driver and lap data.</done>
</task>

<task type="auto">
  <name>Task 2: Add replay-session preload entrypoints to the fetcher</name>
  <files>data_fetcher.py, replay_data.py</files>
  <action>Refactor `F1DataFetcher` so the historical load path can fetch session metadata, drivers, and lap rows once and return a replay-ready object. Keep existing methods usable for the current app, but add a `load_replay_session` style entrypoint that validates the session key, populates driver mappings, loads lap data for all available drivers, and delegates normalization to the new module instead of leaving raw DataFrames as the only contract.</action>
  <verify>python -m py_compile data_fetcher.py replay_data.py</verify>
  <done>A single fetcher call can preload a historical session into reusable replay data without needing the UI loop to orchestrate all intermediate steps.</done>
</task>

<task type="auto">
  <name>Task 3: Document loader assumptions in code and preserve compatibility seams</name>
  <files>data_fetcher.py, replay_data.py</files>
  <action>Add concise comments or docstrings at the replay preload seam that make the lap-granular replay assumption explicit, especially how duplicate rows, missing fields, and ordering are handled. Preserve backward-compatible fetcher methods that Plan 02 can still call while migrating `app.py` off the older manual preload path.</action>
  <verify>rg -n "lap-granular|ReplaySession|load_replay_session" data_fetcher.py replay_data.py</verify>
  <done>The new replay foundation is self-describing enough for Plan 02 and Plan 03 executors to use without re-deriving the intended semantics.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m py_compile data_fetcher.py replay_data.py` passes.
- [ ] A quick local smoke script can instantiate `F1DataFetcher` and confirm the new replay preload entrypoint is importable.
- [ ] The load path no longer requires `app.py` to manually fetch and normalize raw session, driver, and lap payloads before a replay contract exists.
</verification>

<success_criteria>
- All tasks completed
- Historical replay loading has a reusable non-UI contract
- The plan creates a clear input surface for replay snapshot helpers
- No existing fetcher APIs required by the Streamlit app are removed without replacement
</success_criteria>

<output>
After completion, create `.planning/phases/01-replay-data-foundation/01-01-SUMMARY.md`
</output>
