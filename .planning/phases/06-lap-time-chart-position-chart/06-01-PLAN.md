---
phase: 06-lap-time-chart-position-chart
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/DriverToggle/useVisibleDrivers.ts
  - frontend/src/components/DriverToggle/useVisibleDrivers.test.ts
  - frontend/src/components/DriverToggle/DriverToggle.tsx
  - frontend/src/components/LapTimeChart/useLapTimeData.ts
  - frontend/src/components/LapTimeChart/useLapTimeData.test.ts
  - frontend/src/components/LapTimeChart/LapTimeChart.tsx
  - frontend/src/components/Dashboard/Dashboard.tsx
autonomous: true
requirements:
  - ENHANCE-03
  - STRAT-02
  - STRAT-03
  - ENHANCE-02

must_haves:
  truths:
    - "User can toggle individual drivers on/off via a team-grouped checkbox panel"
    - "User can view a lap time scatter chart for selected drivers with outlier laps at 30% opacity"
    - "User can see per-stint linear trend lines overlaid on the lap time chart"
    - "SC/VSC periods appear as shaded regions on the lap time chart"
    - "Replay cursor appears on the lap time chart synced to current lap"
    - "Default visibility is top 2 drivers by lap 1 position"
  artifacts:
    - path: "frontend/src/components/DriverToggle/useVisibleDrivers.ts"
      provides: "Shared visible-drivers state hook"
      exports: ["useVisibleDrivers"]
    - path: "frontend/src/components/DriverToggle/DriverToggle.tsx"
      provides: "Team-grouped checkbox panel UI"
      exports: ["DriverToggle"]
    - path: "frontend/src/components/LapTimeChart/useLapTimeData.ts"
      provides: "Lap time scatter traces, trend line traces, SC shapes"
      exports: ["useLapTimeData", "linearRegression", "isOutlierLap", "buildLapTimeTraces", "computeAllTrendLines"]
    - path: "frontend/src/components/LapTimeChart/LapTimeChart.tsx"
      provides: "Plotly scatter chart rendering lap times"
      exports: ["LapTimeChart"]
  key_links:
    - from: "frontend/src/components/DriverToggle/useVisibleDrivers.ts"
      to: "useSessionStore"
      via: "reads laps + drivers for default selection"
      pattern: "useSessionStore.*laps|drivers"
    - from: "frontend/src/components/LapTimeChart/useLapTimeData.ts"
      to: "useSessionStore"
      via: "reads laps, drivers, currentLap, safetyCarPeriods"
      pattern: "useSessionStore"
    - from: "frontend/src/components/LapTimeChart/useLapTimeData.ts"
      to: "frontend/src/lib/plotlyShapes.ts"
      via: "makeReplayCursorShape for cursor"
      pattern: "makeReplayCursorShape"
    - from: "frontend/src/components/Dashboard/Dashboard.tsx"
      to: "DriverToggle + LapTimeChart"
      via: "visibleDrivers prop threading"
      pattern: "visibleDrivers"
---

<objective>
Build the driver visibility toggle and lap time chart with per-stint trend lines.

Purpose: Gives users control over which drivers appear in multi-driver charts, and shows per-driver pace trends with tire degradation visualization. The DriverToggle is shared infrastructure for both Phase 6 charts.

Output: DriverToggle component, useLapTimeData hook with linearRegression, LapTimeChart component, all wired into Dashboard.
</objective>

<execution_context>
@/Users/luckleineschaars/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luckleineschaars/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-lap-time-chart-position-chart/06-CONTEXT.md
@.planning/phases/06-lap-time-chart-position-chart/06-RESEARCH.md

@frontend/src/types/session.ts
@frontend/src/stores/sessionStore.ts
@frontend/src/lib/plotlyShapes.ts
@frontend/src/components/GapChart/useGapData.ts
@frontend/src/components/StintTimeline/useStintData.ts
@frontend/src/components/Dashboard/Dashboard.tsx

<interfaces>
<!-- Key types and contracts the executor needs -->

From frontend/src/types/session.ts:
```typescript
export interface LapRow {
  LapNumber: number | null
  Driver: string
  Team: string | null
  LapTime: number | null
  Time: number | null
  PitInTime: number | null
  PitOutTime: number | null
  Compound: string | null
  TyreLife: number | null
  Position: number | null
  Stint: number | null
}

export interface DriverInfo {
  abbreviation: string
  fullName: string
  team: string
  teamColor: string
}

export interface SafetyCarPeriod {
  start_lap: number
  end_lap: number
  type: 'SC' | 'VSC' | 'RED'
}
```

From frontend/src/stores/sessionStore.ts:
```typescript
// Zustand selectors used by chart hooks:
const laps = useSessionStore((s) => s.laps)           // LapRow[]
const drivers = useSessionStore((s) => s.drivers)      // DriverInfo[]
const currentLap = useSessionStore((s) => s.currentLap) // number
const safetyCarPeriods = useSessionStore((s) => s.safetyCarPeriods) // SafetyCarPeriod[]
```

From frontend/src/lib/plotlyShapes.ts:
```typescript
export function makeReplayCursorShape(currentLap: number): Partial<Plotly.Shape> | null
```

From frontend/src/components/GapChart/useGapData.ts:
```typescript
export interface TeamDrivers { team: string; drivers: string[] }
export function useDriverList(): { teams: TeamDrivers[] }
```

Plotly CJS interop boilerplate (copy verbatim to new chart components):
```typescript
import _Plot from 'react-plotly.js'
const Plot = (typeof (_Plot as any).default === 'function' ? (_Plot as any).default : _Plot) as typeof _Plot
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DriverToggle hook + component and useLapTimeData hook with tests</name>
  <files>
    frontend/src/components/DriverToggle/useVisibleDrivers.ts,
    frontend/src/components/DriverToggle/useVisibleDrivers.test.ts,
    frontend/src/components/DriverToggle/DriverToggle.tsx,
    frontend/src/components/LapTimeChart/useLapTimeData.ts,
    frontend/src/components/LapTimeChart/useLapTimeData.test.ts
  </files>
  <behavior>
    useVisibleDrivers tests (useVisibleDrivers.test.ts — test pure helper functions):
    - computeDefaultVisible: returns top 2 drivers by lap 1 Position from laps array
    - computeDefaultVisible: returns empty set when laps is empty
    - computeDefaultVisible: handles ties by taking first two sorted

    useLapTimeData tests (useLapTimeData.test.ts — test pure exported functions):
    - linearRegression: returns correct slope/intercept for simple data ([1,2,3], [2,4,6] -> slope=2, intercept=0)
    - linearRegression: returns null for fewer than 2 points
    - isOutlierLap: returns true for lap 1
    - isOutlierLap: returns true when PitInTime is not null
    - isOutlierLap: returns true when PitOutTime is not null
    - isOutlierLap: returns true when lap is within a safety car period
    - isOutlierLap: returns false for a clean racing lap
    - buildLapTimeTraces: returns one scatter trace per visible driver
    - buildLapTimeTraces: scatter markers have opacity 0.3 for outlier laps, 1.0 for clean laps
    - buildLapTimeTraces: filters to laps <= currentLap (progressive reveal)
    - buildLapTimeTraces: skips laps with LapTime === null
    - computeAllTrendLines: returns one trend line per driver-stint with slope/intercept
    - computeAllTrendLines: excludes outlier laps from regression input
    - computeAllTrendLines: returns null for stints with < 2 clean laps
    - buildSCShapes: produces rect shapes for SC periods with progressive reveal clamping
  </behavior>
  <action>
    **useVisibleDrivers.ts:**
    Create hook managing a `Set<string>` of visible driver abbreviations. Export a pure `computeDefaultVisible(laps: LapRow[]): Set<string>` that finds top 2 drivers by Position at LapNumber === 1. The hook uses `useState` initialized from `computeDefaultVisible`, with a `useEffect` that resets only when transitioning from empty->populated laps (guard: skip if visibleDrivers is non-empty and laps length hasn't changed from 0). Export `toggleDriver(abbreviation: string)` that adds/removes from the set.

    **DriverToggle.tsx:**
    Presentational component receiving `visibleDrivers: Set<string>`, `onToggle: (abbr: string) => void`, and using `useDriverList()` from `useGapData.ts` for team-grouped driver list. Render a horizontal flex panel with team name headers and checkboxes per driver. Each checkbox label colored with `teamColor` from `drivers` array (read from `useSessionStore`). Compact horizontal layout with `flex-wrap`. Style: dark background card matching existing UI (`bg-card border border-border rounded-lg p-4`).

    **useLapTimeData.ts:**
    Export pure functions: `linearRegression(xs, ys)` returning `{slope, intercept} | null`, `isOutlierLap(lap, safetyCarPeriods)` returning boolean, `computeAllTrendLines(laps, drivers, safetyCarPeriods)` returning per-driver-stint trend data, `buildLapTimeTraces(...)` returning scatter + trend traces, `buildSCShapes(safetyCarPeriods, currentLap)` returning SC rect shapes.

    Hook follows three-memo split:
    - Memo 1 on `[laps, drivers, safetyCarPeriods]`: compute `allLapsByDriver` (Map of driver -> LapRow[]), `allTrendLines` (per stint regression results)
    - Memo 2 on `[allLapsByDriver, allTrendLines, visibleDrivers, currentLap, ...]`: build scatter traces (type `scattergl`, mode `markers`, marker.opacity array with 0.3/1.0, marker.size 6, color from teamColor) + trend line traces (type `scatter`, mode `lines`, 2-point lines from stint start to end) + SC shapes
    - Memo 3 on `[currentLap]`: cursor shape via `makeReplayCursorShape`

    Scatter traces: one per visible driver. x = LapNumber, y = LapTime (seconds). Filter to `LapNumber <= currentLap`. Skip laps with `LapTime === null`. Hover template: `{driver}<br>Lap %{x}<br>%{y:.3f}s<extra></extra>`.

    Trend lines: for each visible driver, for each stint, run `linearRegression` on clean laps only. Draw a 2-point line from stint start lap to min(stint end lap, currentLap). Color = driver's teamColor. Use `hoverinfo: 'skip'`, `showlegend: false`.

    SC shapes: copy pattern from useGapData.ts lines 261-306 (progressive reveal, fillcolor by type).

    **Tests:** Write tests FIRST for all pure functions. Use the pure-function export pattern from useStintData (no React mocking needed). Create mock LapRow helpers for test data.
  </action>
  <verify>
    <automated>cd /Users/luckleineschaars/repos/f1-dashboard/frontend && npm test -- --reporter=verbose</automated>
  </verify>
  <done>
    - linearRegression returns correct slope/intercept for known data
    - isOutlierLap correctly classifies pit/SC/lap-1 as outliers
    - buildLapTimeTraces returns scatter traces with correct opacity arrays
    - computeAllTrendLines excludes outlier laps from regression
    - buildSCShapes produces correct shapes with progressive reveal
    - computeDefaultVisible returns top 2 by lap 1 position
    - DriverToggle renders team-grouped checkboxes
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: LapTimeChart component and Dashboard wiring</name>
  <files>
    frontend/src/components/LapTimeChart/LapTimeChart.tsx,
    frontend/src/components/Dashboard/Dashboard.tsx
  </files>
  <action>
    **LapTimeChart.tsx:**
    Presentational Plotly component receiving `visibleDrivers: Set<string>` as prop. Uses `useLapTimeData(visibleDrivers)` to get traces and shapes.

    Use the CJS interop boilerplate (copy from StintTimeline.tsx). Render `<Plot>` with:
    - `data`: spread scatterTraces + trendTraces
    - `layout`: dark theme (template plotly_dark, transparent bg), height 400, xaxis title "Lap", yaxis title "Lap Time (s)", shapes: [...scShapes, ...cursorShapes], showlegend false, hovermode closest, margin { t: 16, r: 8, b: 40, l: 56 }
    - `config`: { responsive: true, displayModeBar: false }

    **Dashboard.tsx:**
    Import DriverToggle, LapTimeChart, useVisibleDrivers. Inside the analysis section (after StintTimeline card):
    1. Add `const { visibleDrivers, toggleDriver } = useVisibleDrivers(...)` at component level
    2. Add DriverToggle card: `<div className="bg-card border border-border rounded-lg p-4"><DriverToggle visibleDrivers={visibleDrivers} onToggle={toggleDriver} /></div>`
    3. Add LapTimeChart card: `<div className="bg-card border border-border rounded-lg p-4"><LapTimeChart visibleDrivers={visibleDrivers} /></div>`

    Order in analysis section: StintTimeline -> DriverToggle -> LapTimeChart.
  </action>
  <verify>
    <automated>cd /Users/luckleineschaars/repos/f1-dashboard/frontend && npx tsc --noEmit && npm test -- --reporter=verbose</automated>
  </verify>
  <done>
    - LapTimeChart renders a Plotly scatter chart with dark theme
    - Dashboard shows DriverToggle between StintTimeline and LapTimeChart
    - TypeScript compiles without errors
    - All existing + new tests pass
  </done>
</task>

</tasks>

<verification>
- `cd /Users/luckleineschaars/repos/f1-dashboard/frontend && npm test -- --reporter=verbose` — all tests pass
- `cd /Users/luckleineschaars/repos/f1-dashboard/frontend && npx tsc --noEmit` — no type errors
- DriverToggle shows team-grouped checkboxes defaulting to top 2 drivers
- LapTimeChart shows scatter points with outliers dimmed and trend lines per stint
- SC/VSC shading appears on lap time chart
- Replay cursor tracks current lap on the chart
</verification>

<success_criteria>
- linearRegression pure function tested and correct
- Outlier classification (pit/SC/lap1) tested and correct
- Scatter traces show per-point opacity (0.3 outlier, 1.0 clean)
- Trend lines computed from clean laps only, one per driver-stint
- SC shapes with progressive reveal on lap time chart
- DriverToggle defaults to top 2 drivers, toggles work
- Dashboard layout: StintTimeline -> DriverToggle -> LapTimeChart
- All tests green, TypeScript compiles
</success_criteria>

<output>
After completion, create `.planning/phases/06-lap-time-chart-position-chart/06-01-SUMMARY.md`
</output>
