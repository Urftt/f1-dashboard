---
phase: 02-replay-engine-and-controls
plan: "03"
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - app.py
  - replay_controls.py
  - data_processor.py
  - tests/test_replay_controls.py
autonomous: true
requirements:
  - REPL-02
  - REPL-03
  - REPL-04
  - REPL-05
user_setup: []
must_haves:
  truths:
    - "The Streamlit historical-session flow exposes Play, Pause, Resume, speed, and scrub controls backed by explicit replay state."
    - "Historical replay advances through an explicit Streamlit-native tick path and no longer runs inside the existing blocking while loop."
    - "Charts and KPI cards render from replay-position state and filtered cached history so they stay aligned after play, pause, speed changes, and scrubs."
  artifacts:
    - path: "app.py"
      provides: "Streamlit replay control wiring, session-state initialization, and historical-session rendering based on explicit replay state."
      min_lines: 260
      contains: "replay_status"
    - path: "replay_controls.py"
      provides: "Controller callbacks or helper entrypoints consumed by the app for timed playback and manual controls."
      contains: "advance_replay_state"
    - path: "tests/test_replay_controls.py"
      provides: "Regression coverage for app-facing replay flows and visible-history alignment."
      contains: "filter_interval_history_for_replay"
  key_links:
    - from: "app.py"
      to: "replay_controls.py"
      via: "button, slider, and timed playback code delegate state transitions to controller helpers"
      pattern: "play|pause|resume|speed|scrub"
    - from: "app.py"
      to: "data_processor.py"
      via: "rendering code filters cached interval history and resolves KPI snapshots from replay_position_lap"
      pattern: "filter_interval_history_for_replay|get_replay_snapshot"
---

<objective>
Wire the replay controller into the Streamlit historical-session flow so users can play, pause, resume, change speed, and scrub without breaking the dashboard state.

Purpose: The prior plans establish deterministic helper behavior; this plan replaces the old blocking replay path with an executable UI flow that satisfies the full Phase 2 goal.
Output: Streamlit integration, control callbacks, and final replay-flow verification for all Phase 2 requirements.
</objective>

<execution_context>
@/home/james-turing/.codex/get-shit-done/workflows/execute-plan.md
@/home/james-turing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-replay-engine-and-controls/02-RESEARCH.md
@.planning/phases/02-replay-engine-and-controls/02-VALIDATION.md
@.planning/phases/02-replay-engine-and-controls/02-01-SUMMARY.md
@.planning/phases/02-replay-engine-and-controls/02-02-SUMMARY.md
@app.py
@data_processor.py
@replay_controls.py
@tests/test_replay_controls.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the blocking historical replay flow with explicit control wiring</name>
  <files>app.py, replay_controls.py</files>
  <action>Refactor the historical-session branch in `app.py` so loading a session initializes replay state at lap 1, precomputes or caches full interval history for the selected driver pair, and removes the old `is_tracking`-driven blocking `while` loop from the historical path. Implement the playback trigger explicitly using `st.fragment(run_every=...)` for historical replay ticks, or if Streamlit layout constraints make that unworkable, implement a clearly-scoped fallback rerun mechanism documented in code comments and the summary. Add explicit Play, Pause, Resume, Restart, speed, and scrub controls that mutate `st.session_state` through controller helpers rather than hand-editing ad hoc fields.</action>
  <verify>python -m py_compile app.py replay_controls.py</verify>
  <done>The historical replay flow is driven by explicit controller state, advances through a defined Streamlit tick path, and no longer jumps directly to the final lap or depends on the blocking loop.</done>
</task>

<task type="auto">
  <name>Task 2: Render charts and KPI state from replay-position-driven derived views</name>
  <files>app.py, data_processor.py</files>
  <action>Update the chart and metric rendering paths so they always read the current replay lap from controller state, derive a filtered interval-history prefix for that lap, and keep the tyre/position snapshot cards aligned with the visible chart. Ensure driver-pair changes invalidate or refresh cached interval history cleanly, and keep recorded or live-session behavior isolated unless a minimal compatibility adjustment is required.</action>
  <verify>python -m py_compile app.py data_processor.py</verify>
  <done>The dashboard view remains internally consistent after play, pause, resume, speed changes, or scrubbing because every replay-dependent component reads the same lap state.</done>
</task>

<task type="auto">
  <name>Task 3: Add final regression coverage and executable verification notes</name>
  <files>tests/test_replay_controls.py, app.py</files>
  <action>Extend automated coverage for the app-facing seams that prove the historical replay refactor actually landed: historical session initialization starts at lap 1, first Play does not jump to the final lap, driver-pair changes invalidate or refresh cached pair history, and visible-lap filtering stays aligned with the selected replay position. Where direct Streamlit timer assertions remain awkward, add concise manual verification notes in code comments or summary guidance so the executor can validate Play, Pause, Resume, speed, and scrub behavior against the phase validation contract without guesswork.</action>
  <verify>python -m pytest tests/test_replay_controls.py && python -m pytest</verify>
  <done>The phase has a runnable automated regression path that proves the historical replay path no longer jumps to the finish or depends on the old blocking loop, plus explicit manual checks for the remaining Streamlit-only timing behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m py_compile app.py replay_controls.py data_processor.py` passes.
- [ ] `python -m pytest tests/test_replay_controls.py` passes.
- [ ] `python -m pytest` passes or any unrelated pre-existing failures are documented in the summary.
- [ ] Automated app-facing seam tests cover lap-1 initialization, first-play behavior, and driver-pair cache invalidation.
- [ ] The historical session branch no longer depends on the old `is_tracking` blocking loop and advances replay through an explicit Streamlit tick path.
- [ ] Manual Streamlit verification confirms Play, Pause, Resume, speed change, and scrub/jump controls behave as described in `02-VALIDATION.md`.
</verification>

<success_criteria>
- All tasks completed
- `REPL-02` through `REPL-05` are satisfied through a real Streamlit replay-control flow
- Historical replay state is explicit, durable across reruns, and aligned with chart and KPI rendering
- Phase 3 can consume replay-position state directly without reworking playback semantics again
</success_criteria>

<output>
After completion, create `.planning/phases/02-replay-engine-and-controls/02-03-SUMMARY.md`
</output>
