---
phase: 02-replay-engine-and-controls
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - replay_controls.py
  - data_processor.py
  - tests/conftest.py
  - tests/test_replay_controls.py
autonomous: true
requirements:
  - REPL-02
  - REPL-03
user_setup: []
must_haves:
  truths:
    - "Historical replay state starts at race start instead of the final lap."
    - "Replay progression is derived from explicit controller state, not from a blocking while loop."
    - "Pause and resume preserve the effective replay lap exactly across reruns."
  artifacts:
    - path: "replay_controls.py"
      provides: "Pure replay controller helpers for initialization, play, pause, resume, and lap advancement."
      min_lines: 140
      contains: "def initialize_replay_state"
    - path: "data_processor.py"
      provides: "Replay-view helpers that clamp lap state and slice full interval history to the visible replay prefix."
      contains: "def filter_interval_history_for_replay"
    - path: "tests/test_replay_controls.py"
      provides: "Deterministic unit coverage for start, tick, pause, and resume semantics."
      min_lines: 120
      contains: "test_"
  key_links:
    - from: "tests/test_replay_controls.py"
      to: "replay_controls.py"
      via: "clock-driven replay controller assertions use helper entrypoints directly"
      pattern: "initialize_replay_state|start|pause|resume|advance"
    - from: "data_processor.py"
      to: "replay_controls.py"
      via: "replay lap resolution and visible-history filtering share the same lap-granular controller contract"
      pattern: "replay|lap|history"
---

<objective>
Introduce a pure replay controller contract that can start from race start, advance over wall-clock time, and pause or resume without losing position.

Purpose: Phase 2 needs deterministic replay-state primitives before Streamlit controls can be made reliable.
Output: A controller helper module plus replay-view helpers and tests that lock the base semantics for REPL-02 and REPL-03.
</objective>

<execution_context>
@/home/james-turing/.codex/get-shit-done/workflows/execute-plan.md
@/home/james-turing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-replay-engine-and-controls/02-RESEARCH.md
@.planning/phases/02-replay-engine-and-controls/02-VALIDATION.md
@.planning/phases/01-replay-data-foundation/01-01-SUMMARY.md
@.planning/phases/01-replay-data-foundation/01-02-SUMMARY.md
@.planning/phases/01-replay-data-foundation/01-03-SUMMARY.md
@data_processor.py
@app.py
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a pure replay controller state module</name>
  <files>replay_controls.py</files>
  <action>Add a dedicated replay-control module that owns lap-granular playback state for historical sessions. Define helpers to initialize state at lap 1 after session load, anchor playback to a wall-clock start time, compute the effective replay lap from elapsed time, stop automatically at session end, and represent status explicitly as stopped, playing, or paused. Keep all clock math injectable so tests can pass fixed timestamps instead of relying on datetime.now().</action>
  <verify>python -m py_compile replay_controls.py</verify>
  <done>The repo has a reusable replay controller contract that can start playback from race start and derive the current replay lap without a blocking loop.</done>
</task>

<task type="auto">
  <name>Task 2: Add replay-view helpers for visible history and stable lap resolution</name>
  <files>data_processor.py, replay_controls.py</files>
  <action>Extend the processing layer with helpers that consume the controller state and derive the replay-visible view of precomputed interval history. Add a narrow helper that slices pairwise history to laps less than or equal to the current replay position, and make the lap clamping/default behavior consistent with the new controller semantics so the app no longer infers replay position from interval history maxima.</action>
  <verify>python -m py_compile data_processor.py replay_controls.py</verify>
  <done>The processing layer can render a replay prefix from deterministic controller state instead of treating interval history as mutable playback state.</done>
</task>

<task type="auto">
  <name>Task 3: Lock start, tick, pause, and resume behavior with deterministic tests</name>
  <files>tests/conftest.py, tests/test_replay_controls.py</files>
  <action>Create replay-control fixtures and add unit tests for initialization at lap 1, lap advancement from elapsed time, automatic stop at max lap, pause preserving the current lap, and resume continuing from the paused lap rather than restarting. Use the Phase 1 replay fixture contract where possible so the tests exercise the same replay-session shape as the app.</action>
  <verify>python -m pytest tests/test_replay_controls.py -k "start or tick or pause or resume"</verify>
  <done>Base playback semantics for REPL-02 and REPL-03 are covered by fast, deterministic tests before UI wiring begins.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m py_compile replay_controls.py data_processor.py` passes.
- [ ] `python -m pytest tests/test_replay_controls.py -k "start or tick or pause or resume"` passes.
- [ ] Replay initialization defaults historical sessions to lap 1 and no helper depends on a blocking Streamlit loop.
</verification>

<success_criteria>
- All tasks completed
- `REPL-02` has a durable controller seam for advancing replay time
- `REPL-03` pause and resume semantics are deterministic and test-covered
- Later plans can build UI controls on top of pure helper state instead of ad hoc session-state mutations
</success_criteria>

<output>
After completion, create `.planning/phases/02-replay-engine-and-controls/02-01-SUMMARY.md`
</output>
