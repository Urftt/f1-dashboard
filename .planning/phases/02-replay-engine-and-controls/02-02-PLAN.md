---
phase: 02-replay-engine-and-controls
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - replay_controls.py
  - data_processor.py
  - tests/test_replay_controls.py
autonomous: true
requirements:
  - REPL-04
  - REPL-05
user_setup: []
must_haves:
  truths:
    - "Changing replay speed rebases from the current effective lap instead of retroactively stretching earlier playback."
    - "Scrubbing or jump controls clamp to the session boundary and update replay position immediately."
    - "Historical interval history is treated as cached source data and filtered for the visible replay lap instead of appended over time."
  artifacts:
    - path: "replay_controls.py"
      provides: "Controller helpers for speed changes, scrubbing, jump-to-start/end, and end-state rebasing."
      contains: "def set_replay_speed"
    - path: "data_processor.py"
      provides: "Pair-history filtering helpers aligned with lap-granular replay state."
      contains: "filter_interval_history_for_replay"
    - path: "tests/test_replay_controls.py"
      provides: "Unit coverage for speed rebasing, scrub clamping, and jump semantics."
      contains: "speed"
  key_links:
    - from: "tests/test_replay_controls.py"
      to: "replay_controls.py"
      via: "tests confirm speed and scrub mutations preserve or clamp the effective lap correctly"
      pattern: "set_replay_speed|scrub|jump"
    - from: "replay_controls.py"
      to: "data_processor.py"
      via: "controller state drives which interval-history prefix is visible after speed or scrub actions"
      pattern: "history|lap"
---

<objective>
Extend the replay controller so playback speed changes and lap scrubbing are deterministic, bounded, and compatible with precomputed interval history.

Purpose: Phase 2 is incomplete unless replay can be redirected mid-session without breaking state continuity.
Output: Controller helpers and tests for REPL-04 and REPL-05, plus replay-view filtering that supports speed changes and manual jumps cleanly.
</objective>

<execution_context>
@/home/james-turing/.codex/get-shit-done/workflows/execute-plan.md
@/home/james-turing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-replay-engine-and-controls/02-RESEARCH.md
@.planning/phases/02-replay-engine-and-controls/02-VALIDATION.md
@.planning/phases/02-replay-engine-and-controls/02-01-SUMMARY.md
@data_processor.py
@replay_controls.py
@app.py
@tests/test_replay_controls.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebase replay speed changes on the current effective lap</name>
  <files>replay_controls.py</files>
  <action>Implement a speed-change helper that first resolves the effective replay lap at the moment the speed is changed, then resets the playback anchor so the next ticks continue from that lap at the new rate. Preserve paused-state behavior by allowing speed updates while paused without causing playback to start implicitly, and keep replay-speed values constrained to the UI-supported range.</action>
  <verify>python -m py_compile replay_controls.py</verify>
  <done>Replay speed can change mid-run without jumping backward, replaying earlier laps, or forcing a restart from lap 1.</done>
</task>

<task type="auto">
  <name>Task 2: Add scrub and jump helpers that preserve controller invariants</name>
  <files>replay_controls.py, data_processor.py</files>
  <action>Add explicit helpers for scrubbing to an arbitrary lap, jumping to the start, and jumping to the finish. Clamp every requested lap to the replay-session bounds, keep status transitions deterministic after a manual jump, and ensure the visible interval-history slice and replay snapshots can be derived immediately from the new lap regardless of whether playback was previously playing or paused.</action>
  <verify>python -m py_compile replay_controls.py data_processor.py</verify>
  <done>The controller can handle REPL-05 interactions cleanly and the replay-view layer can render the correct prefix after any jump.</done>
</task>

<task type="auto">
  <name>Task 3: Add deterministic tests for speed, scrub, and jump semantics</name>
  <files>tests/test_replay_controls.py</files>
  <action>Extend the replay-controls test suite with cases that prove speed changes rebase from the current lap, scrubbing clamps to the valid session range, jump-to-start and jump-to-end land on the correct lap, and manual scrub behavior does not silently discard replay state needed by later UI integration. Include at least one assertion that visible interval history is a filtered prefix rather than a separately accumulated dataset.</action>
  <verify>python -m pytest tests/test_replay_controls.py -k "speed or scrub or jump"</verify>
  <done>`REPL-04` and `REPL-05` semantics are guarded by fast helper-level tests before the Streamlit UI starts mutating session state through these paths.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m py_compile replay_controls.py data_processor.py` passes.
- [ ] `python -m pytest tests/test_replay_controls.py -k "speed or scrub or jump"` passes.
- [ ] Replay speed changes and lap jumps preserve the controller contract and use filtered full-history views instead of mutating history incrementally.
</verification>

<success_criteria>
- All tasks completed
- `REPL-04` has a stable speed-change implementation seam
- `REPL-05` has deterministic scrub and jump semantics with session-boundary clamping
- The app can integrate controls later without treating interval-history DataFrames as mutable replay state
</success_criteria>

<output>
After completion, create `.planning/phases/02-replay-engine-and-controls/02-02-SUMMARY.md`
</output>
