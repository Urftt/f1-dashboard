---
phase: 01-backend-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/tsconfig.json
  - frontend/tsconfig.app.json
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/index.html
autonomous: true
requirements:
  - SESS-04

must_haves:
  truths:
    - "App opens in browser and displays a placeholder page without errors"
    - "Vite dev server proxies /api requests to localhost:8000"
    - "Frontend builds successfully with no TypeScript or bundler errors"
  artifacts:
    - path: "frontend/vite.config.ts"
      provides: "Vite config with /api proxy to localhost:8000"
      contains: "proxy"
    - path: "frontend/package.json"
      provides: "React project with Tailwind, shadcn/ui, Zustand, fetch-event-source dependencies"
      contains: "zustand"
    - path: "frontend/src/App.tsx"
      provides: "Root React component"
      exports: ["App"]
  key_links:
    - from: "frontend/vite.config.ts"
      to: "http://localhost:8000"
      via: "proxy config for /api"
      pattern: "proxy.*localhost:8000"
---

<objective>
Scaffold the React frontend with Vite, Tailwind, shadcn/ui, Zustand, and fetch-event-source. Configure Vite proxy for backend API.

Purpose: Establish the frontend project structure and install all dependencies so that frontend UI work (plan 03) can begin immediately without setup overhead.

Output: Buildable frontend skeleton with all dependencies installed and proxy configured.
</objective>

<execution_context>
@/Users/luckleineschaars/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luckleineschaars/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-backend-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold React frontend with all dependencies</name>
  <files>
    frontend/package.json
    frontend/vite.config.ts
    frontend/tsconfig.json
    frontend/tsconfig.app.json
    frontend/src/main.tsx
    frontend/src/App.tsx
    frontend/index.html
  </files>
  <action>
    - Run `npm create vite@latest frontend -- --template react-ts` (or create manually if directory already exists)
    - Run `npx shadcn@latest init -t vite` in frontend/
    - Run `npx shadcn@latest add select progress button` for needed components
    - Install: `npm install zustand @microsoft/fetch-event-source`
    - Configure vite.config.ts with `/api` proxy to `http://localhost:8000`
    - Verify frontend builds: `cd frontend && npm run build`
    - App.tsx should render a minimal placeholder (e.g., "F1 Dashboard" heading) to confirm the app works
  </action>
  <verify>
    <automated>cd /Users/luckleineschaars/repos/f1-dashboard/frontend && npm run build</automated>
  </verify>
  <done>
    - Frontend builds successfully with Vite
    - shadcn/ui components (Select, Progress, Button) installed
    - Zustand and fetch-event-source installed
    - Vite proxy configured for /api
    - App renders in browser without errors
  </done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` succeeds
- `cd frontend && npx tsc --noEmit` — no TypeScript errors
</verification>

<success_criteria>
- Frontend project scaffolded with React, TypeScript, Vite
- All dependencies installed (Tailwind, shadcn/ui, Zustand, fetch-event-source)
- Vite proxy configured for /api to backend
- Clean build with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-foundation/01-02-SUMMARY.md`
</output>
